import "./Loader.scss";


/**
 * loading component which is responsible for showing the loader while fetching the data from api
 */
const Loader=()=>{

    return(
            <div className="loader"></div>
    )
}

export default Loader;